#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAX = 1e5 + 12;

int main(){
	
	ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    
    int n , w; cin >> n >> w;

    
    vector<int>v(n);
    
	for ( int i = 0; i < n; i++ ) cin >> v[i];
	
	sort(v.begin(),v.end());
	
	int cnt = 0 ;
	
    while( v.size() > 0 ){
      
      if ( v.back() == w ){
      	 cnt++; v.pop_back();
	  }else
	  
	   {

		  	int tmp = w - v.back();
		  	
		  	cnt++; v.pop_back();
		  	
		  	if ( v.size() == 0 ) return 0;
		  	
	
		  	
		  	int low = lower_bound(v.begin(), v.end(), tmp) - v.begin();
		 
			  	if ( v[low] <= tmp ) v.erase(low);		  	    
		      	else
				{
					
				   if ( low == 0 ) continue;
									
				   low--; v.erase(low);
		      					
				}	  	
	  	
	  }
	      	
    	
	}
	
		cout << cnt << "\n";	

	return 0;
	
}




